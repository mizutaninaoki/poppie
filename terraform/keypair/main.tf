#-------------------------------------------------------------------------------------------------------
# EC2へログインする時に使用するpublic keyをAWS側に紐づける。(EC2作成時、このpublic keyがEC2インスタンスに登録される)
#-------------------------------------------------------------------------------------------------------
resource "aws_key_pair" "public" {
  key_name   = "${var.app_name}_key_pair"
  public_key = file(var.public_key_path)
}